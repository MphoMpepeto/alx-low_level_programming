#include <stdio.h>

/**
 * main - serves as an entry point of the program.
 *
 * Return: should return 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
		return (0);
}
