#include "main.h"

/**
 * _sqrt_recursion - a function that finds the sqrt of a number
 * @n: an integer that has a square root
 * Return: the squarerrot of a number
 */

int _sqrt_recursion(int n)
{
	if (n %
