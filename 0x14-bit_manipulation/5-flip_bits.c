#include "main.h"

/**
 * flip_bits - function that returns the number of bits you need to flip
 * @n: a bit to be flipped
 * @m: a bit to be flipped
 *
 * Return: the number of bits
 */

unsigned int flip_bits(unsigned long int n, unsigned long int m)
{

