#include "lists.h"

/**
 * pop_listint - a function that deletes the head of a node
 * @head: a double pointer to the head of a list
 *
 * Return: the head node's data or if list is empty 0
 */

int pop_listint(listint_t **head)
