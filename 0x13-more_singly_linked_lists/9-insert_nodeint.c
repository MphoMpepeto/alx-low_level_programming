#include "lists.h"

/**
 * insert_nodeint_at_index - a function that inserts a new node at a given position in the list
 * @head: a double pointer to the head of a list
 * @idx: index of the list where the nide should be added
 * @n:
 *
 * Return: NULL if node not added
 */

listint_t *insert_nodeint_at_index(listint_t **head, unsigned int idx, int n)
