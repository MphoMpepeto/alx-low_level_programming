#include "lists.h"

/**
 * free_listint_safe - a func that frees a list
 * @h: a double pointer to a head node
 *
 * Return: the size of the freed list 
 */

size_t free_listint_safe(listint_t **h)
{
